# .env.example — copy to .env and fill in your values
# Never commit .env to git!

# === Required ===
GOOGLE_API_KEY=your-google-api-key-here     # Gemini access for both Instructor and Pydantic AI paths
TAVILY_API_KEY=your-tavily-api-key-here     # Web search tool

# === Model selection (optional overrides) ===
PLANNER_MODEL_NAME=gemini-2.5-flash
SYNTHESIZE_MODEL_NAME=gemini-2.5-flash
COMPILER_MODEL_NAME=gemini-2.5-flash

# === Memory ===
CHROMA_PATH=./chroma_db
CHROMA_COLLECTION=dossier_memory
MEMORY_HIT_CONFIDENCE_THRESHOLD=0.7    # min cosine similarity to count as a memory cache hit
MEMORY_WRITE_CONFIDENCE_THRESHOLD=0.7  # min SubResult.confidence to write a chunk to ChromaDB

# === Parallelism ===
MAX_PARALLEL_RESEARCHERS=5

# === Logging ===
LOG_LEVEL=INFO
LOG_FORMAT=text          # "text" (dev) or "json" (prod)
